public class PointLight extends Light {

    public PointLight(Vector3D position, Vector3D color) {
        super(position, color);
    }

    // Du kannst hier noch spezifisches Verhalten für Punktlichter ergänzen
}